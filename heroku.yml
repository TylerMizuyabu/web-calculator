build:
  docker:
    client: ./client/Dockerfile
    server: ./server/Dockerfile
run:
  client: /bin/sh -c "envsubst '\$PORT' < /etc/nginx/conf.d/default.conf > /etc/nginx/conf.d/default.conf" && nginx -g 'daemon off;'
